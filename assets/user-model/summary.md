本篇围绕认证系统的核心数据实体——用户，讨论了用户模型（Drizzle Schema）的完整设计。内容涵盖：为什么认证服务的用户表应只聚焦身份标识与凭据（最小化原则）；UUID 主键的安全优势；基于函数索引的大小写不敏感邮箱唯一性约束；`passwordHash` 字段的命名约定与三层防泄露防御（查询层显式排除、TypeScript 类型阻断、序列化 `@Exclude`）；软删除（`deletedAt`）的设计动机与局部唯一索引的配合使用；以及在 NestJS + Drizzle 中实现 Repository 层时如何从数据源头区分敏感查询与公开查询。
